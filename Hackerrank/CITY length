#Displaying the Shortest City
SELECT CITY , LEN(CITY))
FROM STATION
WHERE LEN(CITY)=(SELECT MIN(LEN(CITY)) FROM STATION)
ORDER BY CITY ASC, LIMIT 1


#Displaying the Longest City
SELECT CITY , LEN(CITY))
FROM STATION
WHERE LEN(CITY)=(SELECT MAX(LEN(CITY)) FROM STATION)
ORDER BY CITY ASC, LIMIT 1
